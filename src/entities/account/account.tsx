import React from 'react';
import styles from './account.module.scss';

type Props = {};

const Account = (props: Props) => {
  return <section className={styles.account}>Account</section>;
};

export default Account;
